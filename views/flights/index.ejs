<%- include('../partials/html-head') %>
<link rel="stylesheet" href="/stylesheets/flights/index.css" />
<%- include('../partials/nav') %>

<main>
  <h1>Flight List</h1>
  <table>
    <thead>
      <tr>
        <th>Airline</th>
        <th>Airport</th>
        <th>Flight Number</th>
        <th>Departs</th>
      </tr>
    </thead>
    <tbody>
      
      <% flights.forEach(flight => { %>
        <tr>
          <td><%= movie.airline %></td>
          <td><%= movie.airport %></td>
          <td><%= movie.flightNo %></td>
          <td><%= movie.departs %></td>
        </tr>     
      <% }) %>
    </tbody>
  </table>
</main>

<%- include('../partials/footer') %>